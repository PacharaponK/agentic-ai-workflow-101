Run the full Jest test suite with coverage.

```bash
npm test
```

To run a specific test file:

```bash
npx jest tests/wallet.test.js
```

To run tests matching a name pattern:

```bash
npx jest --testNamePattern="$ARGUMENTS"
```
