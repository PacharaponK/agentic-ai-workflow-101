Run the seed script standalone to inspect sample wallet data without starting the server.

```bash
node src/seed.js
```

Creates 4 wallets (Alice 12,000 / Bob 4,500 / Charlie 2,200 / Diana 0) with pre-built transaction history and prints their IDs and balances to stdout.
